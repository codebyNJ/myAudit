# Go
/bin/
*.out
.env
.DS_Store
desktop/node_modules/
desktop/src-tauri/target/
desktop/dist/
runs/
web/node_modules/
web/dist/

# embedded UI — built by make ui-build / CI; only placeholder committed
internal/api/web/dist/*
!internal/api/web/dist/.gitkeep
!internal/api/web/dist/index.html

# local sqlite db
myaudit.db*
*.db
*.db-shm
*.db-wal

# local planning docs — not part of the shipped repo
docs/

# stray build artifacts
/serve

# tauri sidecar (built by scripts/build-sidecar.sh / CI)
desktop/src-tauri/binaries/*
!desktop/src-tauri/binaries/.gitkeep
